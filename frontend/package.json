{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/assemble-site.mjs",
    "assemble": "node scripts/assemble-site.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "esbuild": "^0.25.12",
    "javascript-obfuscator": "^4.1.1",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.7"
  }
}
