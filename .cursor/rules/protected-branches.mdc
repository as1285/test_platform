---
description: 永久保留 lkj / master / geshui 分支，禁止删除
alwaysApply: true
---

# 永久保留的分支（有线上服务在跑）

以下三个分支**永远不要删除**（本地、远程、服务器均不可删）：

- **`lkj`** — 当前主开发 / 线上站（如 `lkj.qiyun888.top`）
- **`master`** — 仓库默认分支
- **`geshui`** — 有独立服务器部署在跑

## 允许清理的

仅可清理已合入、无开放 PR、且名称形如 `cursor/*` 的临时功能分支。清理前必须确认：

1. 不在上述三分支名单内
2. 对应 PR 已 MERGED/CLOSED，或明确无用
3. 不要 `git push --delete` 上述三分支，也不要在服务器上 `git branch -D` 它们

若用户说「删掉没用的分支」，默认**排除**这三支。
