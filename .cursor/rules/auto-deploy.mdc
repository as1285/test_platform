---
description: 修改可部署代码后自动部署到 lkj 线上，无需用户点 RUN
alwaysApply: true
---

# 自动部署（test_platform / lkj）

在本仓库中完成**会影响运行结果**的修改后（例如 `backend/`、`frontend/`、`docker-compose.yml`、任一 `Dockerfile`、nginx 配置等），**必须在当前任务内、回复用户之前**自动完成部署，**不要**把部署留给用户手动执行，也**不要**因需权限而停顿等待用户点 RUN。

用户已确认：**以后每次这类修改都自动部署**。

## 标准流程（与提交配合）

若同时适用 `git-commit-after-changes.mdc`：

1. **先** `git commit` + `git push -u origin HEAD`（开发分支 **`lkj`**）
2. **再**部署（见下方）
3. 部署失败时一般保留已提交内容，除非用户要求回滚

## 部署方式（按环境）

### A. 本机已有 Docker（服务器上）

```bash
./scripts/deploy.sh
```

或拉最新后再部署：

```bash
./scripts/pull-and-deploy.sh
```

Shell 需 `required_permissions: ["all"]`（要访问 `docker.sock`）。

### B. Cloud Agent / 无本地 Docker（默认远程）

本开发站在 **`lkj.qiyun888.top`**，代码目录 `/root/test_platform`。推送成功后 **SSH 免密**到服务器执行：

```bash
ssh lkj 'cd /root/test_platform && git fetch origin lkj && git checkout lkj && git pull --ff-only origin lkj && ./scripts/deploy.sh'
```

（`Host lkj` 已在 `~/.ssh/config` 配置；若无别名则用 `ssh root@lkj.qiyun888.top ...`。）

### 失败处理

自行重试一次；仍失败则向用户说明原因与日志要点，**不要**仅说「请运行 deploy.sh」。

## 跳过条件

仅在以下情况可跳过部署：

- 用户明确表示本次不要部署
- 仅改文档、注释、`.cursor/` 规则等不影响运行产物的内容

## 发版自检

若部署后做发码/注册/激活冒烟，必须用 `./scripts/redeploy-selftest.sh`（结束自动清理），或手动后跑 `./scripts/cleanup-redeploy-selftest.sh`。详见 `redeploy-selftest-cleanup.mdc`。可选：`DEPLOY_SELFTEST=1 ./scripts/deploy.sh`。
