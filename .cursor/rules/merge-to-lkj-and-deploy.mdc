---
description: 改完必须合进 lkj 并部署上线，禁止只留草稿 PR
alwaysApply: true
---

# 改完合进 lkj 并部署（用户硬性要求）

用户已确认：**以后所有修改都要合进 `lkj` 并部署上线**，不要停在功能分支 / 草稿 PR。

「合进去」= 合进主开发分支 **`lkj`**（线上 `https://lkj.qiyun888.top`，新服务器 `root@103.106.188.166`，目录 `/root/test_platform`）。

## 完成标准（回复用户之前必须做完）

对纳入版本控制的实质性改动，同一轮内按顺序：

1. **提交并推送**当前工作分支（可仍是 `cursor/...-e04c`）
2. **合进 `lkj`**：将 PR 标为可评审后 **merge**（或等价地把提交推进 `origin/lkj`）；**禁止**只开草稿 PR 就结束任务
3. **部署到新服务器**：`ssh lkj 'cd /root/test_platform && ./scripts/pull-and-deploy.sh'`（详见 `auto-deploy.mdc`；目标 IP `103.106.188.166`，不要用旧机）
4. 向用户说明：已合入 `lkj` + 已部署（可附 commit / 站点）

与 `git-commit-after-changes.mdc`、`auto-deploy.mdc` 一致：先提交推送 → 合进 `lkj` → 再部署。

## 明确禁止

- 改完只留 `DRAFT` PR、不 merge、不部署
- 只说「已开 PR，请自行合并/部署」
- 因草稿状态、权限提示或「等用户确认」而跳过合入/部署（用户本条已默认授权）

## 跳过条件（仅这些）

- 用户本轮明确说「不要合」「不要部署」「先别上线」
- 仅改文档 / 注释 / `.cursor/` 规则等**不影响运行产物**的内容：仍应**合进 `lkj`**，但可跳过部署

## 冲突 / 失败

合入有冲突：在本轮内解决或 rebase 后再 merge，不要把未合入状态留给用户。  
部署失败：按 `auto-deploy.mdc` 重试一次；仍失败则说明日志要点，并保留已合入的 `lkj` 提交。
