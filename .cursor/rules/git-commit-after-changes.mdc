---
description: 完成仓库内实质性修改后提交 Git（本地 commit）
alwaysApply: true
---

# 修改后提交 Git（test_platform）

在本仓库中**对纳入版本控制的文件做了实质性修改**，并在本轮任务收尾前：

1. 在仓库根目录执行 `git status`，确认变更文件与范围。
2. 将**本轮实际改动且应入库**的路径 `git add`，使用语义清晰的提交说明执行 `git commit`（完整句子；可选用 `feat`/`fix` 等前缀）。
3. 不要把无关文件、生成物或未参与本轮任务的改动一并提交；若无任何可提交变更，**不要**制造空提交。

若用户在本轮对话中明确表示**不要提交**、或说明版本库由其本人处理，则跳过。

与 `auto-deploy.mdc` 配合时：在适用情况下可在 **`git commit` 之后**再执行 `./scripts/deploy.sh`（部署失败时一般仍保留已提交内容，除非用户要求回滚）。

**推送**：用户未明确要求 `git push` 时，以本地提交为准；用户要求推送时再执行，并注意凭据与远端策略。
